# 打包工作流

## CLI 单文件

`Build CLI Ubuntu Binary` 工作流会把 CLI 打包成 `jinja-build-linux-x86_64`。产物是 Linux x86_64 单文件，下载后加执行权限即可运行。

工作流可以手动触发，也会在推送 `v*` 标签时触发。手动触发时可以填写一个版本标签，上传的 artifact 名称会带上这个标签。

## 兼容边界

产物在 `ubuntu:16.04` 容器里构建，目标是把 glibc 基线降到 2.23。工作流会在容器里编译带共享库的 Python 3.10，再用它完成打包。产物适合 Ubuntu 16.04 及更新的 x86_64 环境。

严格说不能承诺“任意 Ubuntu”。Ubuntu 14.04 或更旧版本、非 x86_64 架构、musl 系统或需要额外系统动态库的模板代码，都需要单独构建对应产物。

## 本地复现

本地有 Docker 时，可以按工作流里的命令在同一个 Ubuntu 16.04 镜像中构建。这样本地产物和 CI 产物会使用同一条兼容基线。
